import 'share_localizations.dart';

/// The translations for English (`en`).
class ShareLocalizationsEn extends ShareLocalizations {
  ShareLocalizationsEn([String locale = 'en']) : super(locale);

  @override
  String get canNotShare => 'The content could not be shared';
}
